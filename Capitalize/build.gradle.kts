version = "1.2.0"
description = "Capitalize every word in your message."

aliucord.changelog.set(
        """
        
        # 1.2.0
        * Supports v96 (hopefully)
        
        """.trimIndent()
)