version = "2.0.1"
description = "Upload image to your desired image host within Discord."

aliucord.changelog.set(
        """
        2.0.1
        * Fix default regex settings and json logging on API response.

        2.0.0
        * Release plugin

        1.6.1
        * Epic new update

        1.5.1
        * New settings added, check plugin settings info. UITH IS AN UNRELEASED PLUGIN. DON'T ASK FOR HELP WITH BUGS.

        """.trimIndent()
)

aliucord.excludeFromUpdaterJson.set(true)
