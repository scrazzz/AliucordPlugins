version = "1.5.0"
description = "Upload image to an image host."

aliucord.changelog.set(
        """

        # 1.5.0
        * Fix crash when opening settings

        # 1.0.0
        * Initial release

        """.trimIndent()
)