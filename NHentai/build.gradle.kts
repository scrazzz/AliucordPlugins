version = "2.2.0"
description = "nhentai"

aliucord.changelog.set(
        """

        # 2.1.0
        * Fixed wrong ID being shown in "search" command

        # 2.0.1
        * Initial release (Don't ask why)

        """.trimIndent()
)
